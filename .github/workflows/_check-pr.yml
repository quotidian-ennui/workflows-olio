name: Check PR
on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, edited]

permissions: {}

jobs:
  check-pr:
    uses: ./.github/workflows/check-pr-with-trigger.yml
    permissions:
      contents: write
      pull-requests: write
      checks: write
